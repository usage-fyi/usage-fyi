- [ ] I want stats to map my PR creations (per project) and how long it took. I reckon this can be done by scanning through all the llm conversation log and looking at sessions that have a PR created link. It will then measure the time it took from session start to PR creation and session ending. Then this should give us enough data. I want this to be done programmatically (i.e. grep-like logic) without sending conversation data to an LLM. We need a design for this (added 2026-06-01) [triaged: T0001]
